# Issue #15 — react-vite reference host

## Done
- Full Tauri 2 + React + Vite example with plugin registration
- Capabilities: visual-editor:all, modal + visual-inspector windows
- SDK metadata demo + modal WebView
- `npm run tauri:dev` verified (Vite :1420 + cargo run)
- Fixed permission set identifiers (hyphens) for host build
