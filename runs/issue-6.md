# Issue #6 — IPC commands API

## Done
- All V1 commands registered: enable, disable, open, close, toggle, capture, revalidate, clear_session, set_target_webview, pin_target_webview, unpin_target_webview, export_context, get_state
- Hub methods with state emit after mutating commands
- `capture` stub returns error (issue #8)
- 8 hub integration-style unit tests
