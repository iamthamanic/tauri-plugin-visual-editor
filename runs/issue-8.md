# Issue #8 — Screenshot capture

## Done
- Native window capture via xcap (no html2canvas)
- Modes: window, webview, element, region with physical pixel crops
- Default 24px CSS element padding; configurable
- PNG saved to app data screenshots dir
- Async command with 5s timeout; session updated only on success
- Crop math unit tests
